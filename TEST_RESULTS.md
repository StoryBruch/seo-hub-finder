# Test Results

The package was generated and tested with:

```bash
python seo_hub_finder.py sample_gsc.csv --out-dir sample_out_no_volume
python seo_hub_finder.py sample_gsc.csv --volume-csv sample_volume.csv --out-dir sample_out_with_volume
```

Both runs completed and generated CSV, Markdown, HTML and ZIP outputs.
